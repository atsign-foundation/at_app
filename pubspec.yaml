name: at_app
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_tools:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.10.0
