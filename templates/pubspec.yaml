name: templates
description: Flutter app templates built using the atPlatform that can be generated using the at_app package.
version: 1.0.1

publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dev_dependencies:
  flutter_lints: ^1.0.0
